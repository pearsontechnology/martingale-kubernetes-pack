$type: HeaderPage
props:
  title:
    $map: '`${params.namespace} - ${params.name}`'
children:
  $type: Panel
  props:
    inset: true
  children:
    $type: Provider
    props:
      provide:
        json:
          url:
            $map: >-
              (params.env?`/api/${params.env}/kube`:"/api/kube")+`/api/v1/namespaces/${params.namespace}/services/${params.name}`
      Component:
        $component: JsonView
      props:
        pretty: true
paths:
  - /kubernetes/namespace/:namespace/service/:name
  - /kubernetes/:env/namespace/:namespace/service/:name
